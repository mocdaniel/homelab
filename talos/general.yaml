---
machine:
  kubelet:
    image: ghcr.io/siderolabs/kubelet:v1.28.7
  install:
    disk: /dev/mmcblk0
cluster:
  network:
    cni:
      name: none
  proxy:
    disabled: true
  allowSchedulingOnControlPlanes: true
